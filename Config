package.OpenSearch-Dashboards = {
    interfaces = (devstack_platform_3.x);
    build-system = container-build;

    build-environment = {
        network-access = open;
    };

    libfarm = {
        components = ("opensearch-dashboards-core");
    };

    build-tools = {
        devstack_platform_3.x = {
            ContainerBuild = 1.2;
            AWSOpenSearchNeoContainerBuildCredentialsProvider = 1.0;
        };
    };

    # Use runtime-dependencies for when you want to bring in additional
    # packages when deploying.
    # Use dependencies instead if you intend for these dependencies to
    # be exported to other packages that build against you.
    dependencies = {
        devstack_platform_3.x = {
        };
    };

    runtime-dependencies = {
        devstack_platform_3.x = {
        };
    };
};
